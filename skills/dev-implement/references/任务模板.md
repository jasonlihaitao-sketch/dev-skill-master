# {feature}-task-NN — {任务标题}

> 产物路径：`develop/features/{feature}/tasks/{feature}-task-NN-{slug}.md`
> 上游：{feature}-implementation-plan.md（第 4 节本任务的代码级方案）、{feature}-requirement.md、{feature}-design.md
> 下游：dev-review 据第 3 验收点与第 4 测试用例核对覆盖。
> 编号两位起，slug 用 kebab-case 且有意义。
>
> 本模板是**执行期逐任务追踪**：代码级细节在实施方案里，这里记录目标、验收点、测试用例与 TDD 执行状态/证据。
> dev-implement 确认执行后，逐个任务建本文件并推进红绿循环。

## 1. 任务目标

> 一句话。对应实施方案 task-NN。

## 2. 关联

- **实施方案**：`{feature}-implementation-plan.md` 第 4 节 task-NN
- **需求**：`{feature}-requirement.md` 第 4 章 AC{N}
- **方案**：`{feature}-design.md` 第 {4/5/6} 章

## 3. 验收点（对应 requirement 验收标准）

- [ ] 验收点1：Given ...，When ...，Then ...
- [ ] 验收点2：...

## 4. 测试用例（Red 先写）

> 来自实施方案第 4 节本任务的测试用例。执行期 Red 先写这些。

- `{test_name}`（对应验收点1）— {断言意图}
- `{test_name}`（边界/异常）— {断言意图}

## 5. 涉及文件

- [新建/修改] `src/...` — {职责}
- [新建] `tests/...` — {测试职责}

## 6. 状态与 TDD 执行证据

> 状态流转：待执行 → Red 完成 → Green 完成 → Refactor 完成 → 完成。
> 严格红绿，硬检查点见 references/TDD纪律.md。后端无例外；前端 UI 酌情须注明。

- **当前**：待执行
- **TDD 适用**：严格红绿 / 前端 UI 酌情（替代验证：{方式}）
- **Red 证据**：（运行测试失败的命令与输出摘要）
- **Green 证据**：（运行测试通过的命令与输出摘要）
- **Refactor 证据**：（重构后测试仍绿的命令与输出摘要）
