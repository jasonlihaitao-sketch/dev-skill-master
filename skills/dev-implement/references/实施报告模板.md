# {feature} — 实施报告

> 产物路径：`develop/features/{feature}/{feature}-implementation-report.md`
> 上游：{feature}-implementation-plan.md、tasks/、src/、tests/
> 下游：交付依据；dev-review 据此核对实现符合度与 AI 决策差异。
> dev-implement 执行阶段全部任务完成后产出本报告，与测试报告一同交付。

- **报告日期**：YYYY-MM-DD
- **实施范围**：{feature} 的全部任务
- **实施状态**：已完成 / 部分完成 / 已完成含遗留

## 1. 实施总览

- **目标**：（对齐 requirement 第 1 章）
- **完成任务数**：{N}/{N}
- **代码变更**：新建 {N} 文件，修改 {N} 文件
- **实施方案**：`{feature}-implementation-plan.md`
- **实施状态摘要**：（一句话）

## 2. 完成任务清单

| 任务 | 标题 | 状态 | 备注 |
|---|---|---|---|
| task-01 | ... | 完成 | |
| task-02 | ... | 完成 | |
| task-03 | ... | 部分完成 | 遗留：{...} |

## 3. 代码变更总览

> 实际改动的文件清单，与 implementation-plan 第 3.1 节对照。

| 操作 | 路径 | 职责 | 与方案一致 |
|---|---|---|---|
| 新建 | `src/modules/{feature}/{name}.{ext}` | {职责} | ✓ |
| 修改 | `src/...` | {改动} | ✗（见第 5 节） |
| 新建 | `tests/...` | {测试} | ✓ |

## 4. 与实施方案的符合度

> 整体核对实现是否符合实施方案（接口签名、数据结构、类设计、文件结构）。

- **接口签名符合度**：{N}/{N} 一致
- **数据结构符合度**：{N}/{N} 一致
- **类设计符合度**：{N}/{N} 一致
- **文件结构符合度**：{N}/{N} 一致
- **不符合项**：见第 5 节 AI 决策差异

## 5. AI 决策差异（实现层面）

> ⚠️ 重点章节。记录 AI 在执行阶段对**实现**做出的、与原实施方案不同的决策。
> 每条明确标注：决策点、原方案、AI 实际做法、原因、影响。
> 这些决策是 AI 执行期自主决断，与原 implementation-plan 存在差异，需透明披露并供 dev-review 与用户审查。

| 序号 | 决策点 | 原方案 | AI 实际做法 | 原因 | 影响 |
|---|---|---|---|---|---|

示例：
| 1 | login 返回类型 | 原方案返回 `{ token, expiresIn }` | 改为 `{ token, expiresIn, refreshToken }` | 执行中发现前端需刷新 token，design 未覆盖 | 接口扩展，需同步更新 design 第 4 章并通知前端 |
| 2 | 数据访问层 | 原方案直接查库 | 增加缓存层 | Red 阶段测试暴露循环查库性能问题 | 新增 cache 模块，影响 project-context 第 4 章架构 |
| 3 | 任务拆分 | 原方案 task-02 单任务 | 拆为 task-02a + task-02b | 执行中发现依赖复杂，单任务过大 | 任务数变化，已更新 tasks/ |

**需回写的上游文档**：
> AI 决策差异若改变了既定设计，应回写对应上游文档保持一致。列出待回写项。

- [ ] `{feature}-design.md` 第 {N} 章 — {改动原因}
- [ ] `develop/project-context.md` 第 {N} 章 — {改动原因}
- [ ] `{feature}-requirement.md` — {是否需补充验收点}

> 若执行期间 AI 未做任何实现层面偏离，写「无」。

## 6. 遗留问题与技术债

> 未完成的任务、绕过的问题、待优化点，诚实记录。

- 遗留：{问题}，原因：{...}，后续：{...}
- 技术债：{...}

## 7. 验证结果

> 测试是否通过，引用测试报告。

- **测试报告**：`{feature}-test-report.md`
- **测试结果**：{全部通过 / 部分失败}
- **验收点覆盖**：{N}/{N}
- **构建/启动验证**：{build/lint 通过情况}

## 8. 结论

- **实施状态**：{已完成 / 部分完成 / 含遗留}
- **与方案符合度**：{高 / 有差异（见第 5 节）}
- **AI 决策差异数**：{N}（见第 5 节）
- **需回写上游**：{N} 项（见第 5 节）
- **说明**：（一句话，如「3 任务完成，1 项实现决策差异已披露并需回写 design，测试全部通过」）
