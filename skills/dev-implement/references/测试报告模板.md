# {feature} — 测试报告

> 产物路径：`develop/features/{feature}/{feature}-test-report.md`
> 上游：{feature}-implementation-plan.md、tasks/、src/、tests/
> 下游：交付依据；dev-review 据此核对测试覆盖与 AI 决策差异。
> dev-implement 执行阶段全部任务完成后产出本报告。

- **报告日期**：YYYY-MM-DD
- **测试范围**：{feature} 的全部任务与代码
- **测试状态**：全部通过 / 部分失败 / 未完成

## 1. 测试结果总览

| 指标 | 数值 |
|---|---|
| 测试用例总数 | {N} |
| 通过 | {N} |
| 失败 | {N} |
| 跳过/未实现 | {N} |
| 代码覆盖率 | {%} |
| 验收点总数 (AC) | {N} |
| 已覆盖验收点 | {N} |

## 2. 验收点覆盖矩阵

> AC ↔ 任务 ↔ 测试 三者可追溯，与 implementation-plan 第 5.3 节对齐。dev-review 据此核对。

| 验收点 (AC) | 任务 | 测试用例 | 结果 |
|---|---|---|---|
| AC1 | task-01 | `{test_name}` | 通过 |
| AC2 | task-02 | `{test_name}` | 通过 |
| AC3（边界） | task-03 | `{test_name}` | 通过 |

## 3. 测试用例清单与结果

> 逐条列出执行的测试及结果。

| 用例 | 对应验收点 | 类型 | 结果 | 备注 |
|---|---|---|---|---|
| `{test_name}` | AC1 | 正常 | 通过 | |
| `{test_name}` | AC3 | 异常 | 通过 | |

## 4. TDD 执行情况

> 红绿循环节录，验证 TDD 纪律遵守情况。后端无例外，前端 UI 酌情须注明。

| 任务 | Red | Green | Refactor | TDD 适用 |
|---|---|---|---|---|
| task-01 | ✓（先写失败测试） | ✓ | ✓ | 严格红绿 |
| task-02 | ✓ | ✓ | ✓ | 严格红绿 |
| task-03 | ✓ | ✓ | ✓ | 前端 UI 酌情（替代：视觉快照） |

**运行命令与证据**：{test-runner 命令 + 通过输出摘要}

## 5. 覆盖率

- **总体覆盖率**：{%}
- **关键模块覆盖率**：
  - `{module}`：{%}
- **未覆盖部分**：（列出未覆盖的代码与原因，如前端 UI、外部 IO mock 边界）

## 6. 未覆盖与已知问题

> 已知未覆盖的测试点、已知失败、已知缺陷，诚实记录。

- 未覆盖：{点}，原因：{前端 UI / 外部依赖 / 时间}
- 已知失败：{用例}，原因：{...}
- 已知缺陷：{...}

## 7. AI 决策差异（测试层面）

> ⚠️ 重点章节。记录 AI 在执行阶段对**测试**做出的、与原实施方案不同的决策。
> 每条明确标注：决策点、原方案、AI 实际做法、原因、影响。
> 这些决策是 AI 执行期自主决断，与原 implementation-plan 存在差异，需透明披露。

| 序号 | 决策点 | 原方案 | AI 实际做法 | 原因 | 影响 |
|---|---|---|---|---|---|

示例：
| 1 | AC2 测试策略 | 原计划单元测试直接断言返回值 | 改为先 mock 外部服务再断言 | 执行中发现该函数强依赖外部 IO，不 mock 无法稳定测试 | 测试更稳定，但需同步 mock 维护成本 |
| 2 | 边界用例 | 未计划空输入用例 | 新增空密码用例 | Red 阶段发现 requirement AC3 隐含空值场景 | 覆盖更全，已补 AC3 |

> 若执行期间 AI 未做任何测试层面偏离，写「无」。

## 8. 结论

- **测试状态**：{全部通过 / 部分失败 / 未完成}
- **验收点覆盖**：{N}/{N}
- **AI 决策差异数**：{N}（见第 7 节）
- **说明**：（一句话，如「全部 12 用例通过，覆盖率 92%，2 项测试决策差异已披露，2 项前端 UI 未覆盖已记录」）
