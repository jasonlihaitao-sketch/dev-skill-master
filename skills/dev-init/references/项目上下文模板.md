# {项目名} — 项目上下文

> 全流程只读 + 增量更新的载体。后续每个 skill 启动时先读本文件。
> 维护规则：requirement/design/implement 若产生新的全局约定或术语，回写本文件对应章节。

## 1. 项目画像

- **名称**：
- **定位**：（一句话说明这是什么、解决什么问题）
- **目标用户**：
- **核心价值**：

## 2. 技术栈

- **语言**：
- **框架**：
- **运行时**：
- **关键依赖**：（列出对方案有约束的依赖及版本）
- **包管理**：

## 3. 文件结构

> 用目录树 + 每个目录一句话说明。代码在仓库根，流程产物在 develop/。

```text
{仓库根目录树}
```

| 目录 | 说明 |
|---|---|

## 4. 架构

> 分层 / 模块划分 / 核心数据流。

- **分层**：
- **模块划分**：
- **核心数据流**：（复杂时可放 design 里用 mermaid 表达）

## 5. 编码规范

> dev-implement 与 dev-review 据此执行。

**适用前提（重要）**：本节规范是「按栈取用」的参考，不存在的栈不套、不强制。先据第 2 章技术栈判定项目实际用什么：
- 后端为 Java → 参考 `编码规范模板-后端.md`
- 前端为 React/Vue + TypeScript → 参考 `编码规范模板-前端.md`
- 其它栈（如 Python）且无对应模板 → **不套用上述模板**，按项目自身约定执行，此处只填该栈的实际规范。

**跨端通用项（本节填写）**：

- **提交**：（commit message 规范，如 Conventional Commits）
- **lint/format 工具与配置位置**：（如 ESLint + Prettier / Spotless，配置文件路径）
- **通用命名底线**：（跨端通用的命名/格式约定，若各端有专属规则则写在对应模板）

**端专属规范**（按栈取用，把对应模板内容填到下方对应小节；不存在的栈写「本项目无此栈，不适用」）：

### 5.1 后端规范
> 仅当后端为 Java 等有对应模板的栈时填充，据 `编码规范模板-后端.md`。
-

### 5.2 前端规范
> 仅当前端为 React/Vue/TS 等有对应模板的栈时填充，据 `编码规范模板-前端.md`。
-

## 6. 术语表

| 术语 | 含义 |
|---|---|

## 7. 非功能约束

> dev-design 与 dev-review 据此评估。

- **性能**：（响应时间/吞吐/资源上限）
- **安全**：（认证/授权/敏感数据约束）
- **兼容**：（平台/版本/浏览器/设备）
- **可用性**：（SLA/容错）

## 8. 边界（不做什么）

> 明确排除项，防止范围蔓延。

-
-

## 待澄清

> 暂时无法确定、需后续补充的项。
